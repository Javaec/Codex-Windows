{
  "schemaVersion": 1,
  "id": "webview-persist-extended-history",
  "description": "Force persistExtendedHistory=true so older chats continue to load after Codex CLI upgrades.",
  "lane": "webview",
  "priority": 240,
  "conflicts": [],
  "injector": {
    "stageId": "mods",
    "inputContract": "artifact.deobfuscated-app",
    "outputContract": "artifact.patched-app"
  },
  "compatibility": {
    "snapshotRegex": "codex"
  },
  "steps": [
    { "id": "webview-persist-extended-history", "required": true }
  ]
}

