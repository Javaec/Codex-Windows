{
  "schemaVersion": 1,
  "id": "webview-thread-per-project-cap",
  "description": "Rewrite thread/list requests limit=10 -> 6 while keeping Show more/Show less behavior intact.",
  "lane": "webview",
  "priority": 235,
  "conflicts": [],
  "injector": {
    "stageId": "mods",
    "inputContract": "artifact.deobfuscated-app",
    "outputContract": "artifact.patched-app"
  },
  "compatibility": {
    "snapshotRegex": "codex"
  },
  "steps": [
    { "id": "webview-thread-per-project-cap", "required": true }
  ]
}
