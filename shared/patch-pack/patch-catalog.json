{
  "schemaVersion": 1,
  "stepOrder": [
    "preload",
    "webview-sunset",
    "webview-cwd",
    "webview-settings-limits",
    "webview-thread-per-project-cap",
    "webview-persist-extended-history",
    "main-runtime-shim"
  ],
  "steps": {
    "preload": {
      "description": "Inject process bridge into preload bundle.",
      "stage": "preload"
    },
    "webview-sunset": {
      "description": "Bypass forced app sunset gate in webview bundles.",
      "stage": "webview"
    },
    "webview-cwd": {
      "description": "Normalize cwd path comparison helpers in webview bundles.",
      "stage": "webview"
    },
    "webview-settings-limits": {
      "description": "Inject a usage limits panel above Settings in the webview sidebar.",
      "stage": "webview"
    },
    "webview-thread-per-project-cap": {
      "description": "Rewrite the renderer recent section cap (10 -> 6) while keeping Show more/Show less behavior.",
      "stage": "webview"
    },
    "webview-persist-extended-history": {
      "description": "Force persistExtendedHistory=true for thread resume so legacy chats keep loading after CLI upgrades.",
      "stage": "webview"
    },
    "main-runtime-shim": {
      "description": "Inject Windows runtime shim and SQLite path normalization hooks.",
      "stage": "main"
    }
  }
}
