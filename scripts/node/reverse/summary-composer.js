"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.composeReverseSummary = composeReverseSummary;
function composeReverseSummary(input) {
    return {
        generatedAtUtc: input.generatedAtUtc,
        appDir: input.appDir,
        outDir: input.outDir,
        packageName: input.packageJson.name ?? null,
        packageVersion: input.packageJson.version ?? null,
        packageMain: input.packageJson.main ?? null,
        filesIndexed: input.filesIndexed,
        jsFiles: input.jsFiles,
        cssFiles: input.cssFiles,
        htmlFiles: input.htmlFiles,
        importsNodes: input.importsNodes,
        runtimeRpcNoiseMode: input.runtimeRpcNoiseMode,
        decompile: {
            noPretty: input.decompile.noPretty,
            maxPrettyBytes: input.decompile.maxPrettyBytes,
            prettyOk: input.decompile.prettyOk,
            copiedRaw: input.decompile.copiedRaw,
            skippedLarge: input.decompile.skippedLarge,
        },
        parseErrors: input.parseErrors,
        signals: {
            ipcChannels: input.ipcRows.length,
            methods: input.methodRows.length,
            rpcCatalog: input.rpcCatalog.length,
            rpcSchemaMethods: input.rpcSchema.coverage.methods,
            rpcSchemaFromRuntime: input.rpcSchema.coverage.fromRuntime,
            rpcSchemaWithPayload: input.rpcSchema.coverage.withPayloadKeys,
            rpcSchemaWithRendererCallsites: input.rpcSchema.coverage.withRendererCallsites,
            rpcSchemaRuntimeSoftRecoveredMethods: input.rpcSchema.runtimeProbe.softRecoveredMethods,
            rpcEnvelopeRequestMethods: input.rpcSchema.envelopes.request,
            rpcEnvelopeResponseMethods: input.rpcSchema.envelopes.response,
            rpcEnvelopeEventMethods: input.rpcSchema.envelopes.event,
            routes: input.routeRows.length,
            messageTypes: input.messageTypeRows.length,
            statuses: input.statusRows.length,
            stateKeys: input.stateKeyRows.length,
            ipcContractChannels: input.ipcContractMap.channels.length,
            ipcWrapperFiles: input.ipcContractMap.wrappers.filesWithWrappers,
            ipcWrappersDiscovered: input.ipcContractMap.wrappers.wrappersDiscovered,
            ipcWrapperInvocationsResolved: input.ipcContractMap.wrappers.wrapperInvocationsResolved,
            ipcGlobalWrappersDiscovered: input.ipcContractMap.wrappers.globalWrappersDiscovered,
            componentBoundaries: input.componentBoundaries.boundaries.length,
            componentChunks: input.componentBoundaries.chunks.length,
            deobfMappedFiles: input.deobfuscationTable.coverage.mappedFiles,
            deobfMappedSymbols: input.deobfuscationTable.coverage.mappedSymbols,
            deobfFileCandidates: input.deobfuscationTable.coverage.obfuscatedFileCandidates,
            deobfSymbolCandidates: input.deobfuscationTable.coverage.obfuscatedSymbolCandidates,
            deobfVariableCandidates: input.deobfuscationTable.coverage.obfuscatedVariableCandidates,
            deobfMappedVariables: input.deobfuscationTable.coverage.mappedVariables,
            deobfVariableCoveragePercent: input.deobfuscationTable.coverage.variableCoveragePercent,
            deobfLowConfidenceSymbols: input.qualityGates.metrics.lowConfidenceSymbols,
            deobfNoisySymbolNames: input.qualityGates.metrics.noisySymbolNames,
            qualityGatePassed: input.qualityGates.passed ? 1 : 0,
            qualityGateFailures: input.qualityGates.failures.length,
            qualityGateGenericNoise: input.qualityGates.metrics.genericNoisePaths.length,
            sessionFlowRoutes: input.sessionFlow.entries.length,
            routeBoundaryGraphNodes: input.routeBoundaryGraph.nodes.length,
            routeBoundaryGraphEdges: input.routeBoundaryGraph.edges.length,
            routeBoundaryGraphEnvelopes: input.routeBoundaryGraph.coverage.envelopes,
            cssVars: input.designSystem.vars.length,
            cssClasses: input.designSystem.classes.length,
            cssColors: input.designSystem.colors.length,
            runtimeProbeWarningsSystem: input.runtimeProbe.warningClassification.system.length,
            runtimeProbeWarningsLogic: input.runtimeProbe.warningClassification.logic.length,
            runtimeProbeWarningsUnknown: input.runtimeProbe.warningClassification.unknown.length,
            runtimeProbeErrorsSystem: input.runtimeProbe.errorClassification.system.length,
            runtimeProbeErrorsLogic: input.runtimeProbe.errorClassification.logic.length,
            runtimeProbeErrorsUnknown: input.runtimeProbe.errorClassification.unknown.length,
            runtimeProbeCapturedLines: input.runtimeProbe.capturedLines.length,
            referenceParityWeightedCoverage: input.referenceParityGaps.coverage.weightedCoveragePercent,
            referenceParityWeightedGapScore: input.referenceParityGaps.coverage.weightedGapScore,
            referenceParityCritical: input.referenceParityGaps.domains.filter((row) => row.confidenceTier === "critical").length,
            referenceParityHigh: input.referenceParityGaps.domains.filter((row) => row.confidenceTier === "high").length,
            referenceParityMedium: input.referenceParityGaps.domains.filter((row) => row.confidenceTier === "medium").length,
        },
        referenceContext: {
            sourcePath: input.referenceSignals.sourcePath,
            copiedPath: input.referenceSignals.copiedPath,
            loaded: input.referenceSignals.loaded,
            bytes: input.referenceSignals.bytes,
            warningCount: input.referenceSignals.warnings.length,
            priorCounts: {
                routes: input.referenceSignals.keywordGroups.routes.length,
                methods: input.referenceSignals.keywordGroups.methods.length,
                stateKeys: input.referenceSignals.keywordGroups.stateKeys.length,
                readiness: input.referenceSignals.keywordGroups.readiness.length,
                events: input.referenceSignals.keywordGroups.events.length,
                ipc: input.referenceSignals.keywordGroups.ipc.length,
                ui: input.referenceSignals.keywordGroups.ui.length,
            },
        },
        referenceSymbols: {
            loaded: input.referenceSymbols.loaded,
            oneCodePath: input.referenceSymbols.oneCodePath,
            codexMonitorPath: input.referenceSymbols.codexMonitorPath,
            symbolCount: input.referenceSymbols.symbols.length,
            warningCount: input.referenceSymbols.warnings.length,
        },
        referenceModel: {
            generatedAtUtc: input.referenceModel.generatedAtUtc,
            unifiedFiles: input.referenceModel.unified.files.length,
            pathMapEntries: input.referenceModel.unified.pathMap.length,
            domainCount: Object.keys(input.referenceModel.unified.domainKeywords).length,
        },
        deobfuscation: {
            mappedFiles: input.deobfuscationTable.coverage.mappedFiles,
            mappedSymbols: input.deobfuscationTable.coverage.mappedSymbols,
            mappedVariables: input.deobfuscationTable.coverage.mappedVariables,
            variableCoveragePercent: input.deobfuscationTable.coverage.variableCoveragePercent,
            lowConfidenceSymbols: input.qualityGates.metrics.lowConfidenceSymbols,
            noisySymbolNames: input.qualityGates.metrics.noisySymbolNames,
            entries: input.deobfuscationTable.entries.length,
            filePlans: input.deobfuscationTable.filePlans.length,
            referenceLoaded: input.deobfuscationTable.referenceInputs.loaded,
        },
        project: {
            rootPath: input.project.rootPath,
            chunkFiles: input.project.chunkFiles,
            reconstructedFiles: input.project.reconstructedFiles,
            mappedTargets: input.project.mappedTargets,
            mappingArtifacts: input.project.mappingArtifacts,
            checks: input.project.checks,
        },
        qualityGates: input.qualityGates,
        referenceParity: {
            weightedCoveragePercent: input.referenceParityGaps.coverage.weightedCoveragePercent,
            weightedGapScore: input.referenceParityGaps.coverage.weightedGapScore,
            topGapDomain: input.referenceParityGaps.topGaps[0]?.domain ?? null,
            topGapScore: input.referenceParityGaps.topGaps[0]?.gapScore ?? null,
            topGapImpactScore: input.referenceParityGaps.topGaps[0]?.impactScore ?? null,
            topGapTier: input.referenceParityGaps.topGaps[0]?.confidenceTier ?? null,
        },
        runtimeProbe: input.runtimeProbe,
        binary: input.binary
            ? {
                source: input.binary.binaryPath,
                rawMatches: input.binary.rawMatches.length,
                rpcLikeMethods: input.binary.rpcLikeMethods.length,
            }
            : null,
    };
}
